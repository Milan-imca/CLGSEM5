'''
  Note:
  widgets: GUI elements : buttons,textboxes,labels,images
  windows: serves as a container to hold or contain these widgets
'''
from tkinter import *

window = Tk() #window instantiated!
window.geometry("420x420")
window.title("Title Here")
window.config(background='#d9fbca')

window.mainloop() #this will place a window

# for changing the logo at left top:
# icon = PhotoImage(file="pathToFile")
# window.iconphoto(True,icon)



























































































































